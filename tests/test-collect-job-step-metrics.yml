name: Test collect job and step metrics (dry-run)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          github-token: ${{ github.token }}
          collect-job-metrics: 'true'
          collect-step-metrics: 'true'
          # No datadog-api-key → dry-run mode
      - name: Assert action completed with job and step metrics collection (dry-run)
        run: |
          echo "Action completed successfully with job and step metrics collection in dry-run mode"
          echo "PASS"
