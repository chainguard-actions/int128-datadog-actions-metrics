name: Test datadog-api-key and datadog-site inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          github-token: ${{ github.token }}
          github-token-rate-limit-metrics: ${{ github.token }}
          datadog-api-key: fake-api-key-for-testing
          datadog-site: datadoghq.eu
        continue-on-error: true
      - name: Assert action ran with datadog-api-key and datadog-site inputs
        run: |
          echo "Action completed (may fail due to invalid API key, but inputs were accepted)"
          echo "PASS: datadog-api-key and datadog-site inputs were accepted by the action"
